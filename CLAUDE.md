# Claude Code — 프로젝트 지침

## 필수 규칙

### 테스트 빌드 자동 복사

`jonanjadeul/` 파일을 수정하고 커밋·푸시한 뒤 **반드시** 아래 명령어를 실행한다.

```bash
cp -r jonanjadeul/* output/jonanjadeul/TEST/
```

`output/jonanjadeul/TEST/`는 즉시 열어볼 수 있는 테스트 빌드 폴더다.
커밋 후 이 복사 단계를 절대 빠뜨리지 않는다.

### 코드 수정 시 GUIDE.md 반드시 업데이트

모든 코드 변경(버그 수정, 기능 추가, 수치 조정 등)을 완료한 후
**GUIDE.md의 해당 섹션과 수정 이력을 반드시 함께 커밋**한다.

업데이트 기준:
- 버그 수정 → `## 10. 자주 나오는 버그 패턴` 에 추가, `## 11. 수정 이력` 갱신
- 새 클래스/함수 추가 → `## 4. 클래스 & 모듈 지도` 갱신
- CFG/config 수치 변경 → `## 3. 설정값 (CFG)` 갱신
- 상태머신 변경 → `## 5. 개미 상태머신 (FSM)` 갱신
- 렌더링 구조 변경 → `## 9. 렌더링 파이프라인` 갱신

### 버전 관리

`main.js` 3번째 줄의 `VERSION` 상수를 수정 내용이 유의미할 때 올린다.
형식: `vMAJOR.MINOR.PATCH`

### config.js

게임 수치(개미 수, 비용, HP 등)를 변경할 때는 `main.js`의 CFG가 아닌
**`config.js`를 수정**한다. `config.js`의 값이 CFG 기본값을 덮어쓴다.

### 커밋 메시지 형식

```
한 줄 요약 (무엇을 왜)

- 세부 변경사항 1
- 세부 변경사항 2
```

## 파일별 역할 요약

| 파일 | 수정해야 할 때 |
|---|---|
| `config.js` | 게임 밸런스 수치 조정 |
| `colony.js` | 식민지 경제·업그레이드·UI 바인딩 |
| `main.js` | AI 로직·렌더링·전투 시스템 |
| `index.html` | UI 레이아웃·도움말·패널 |
| `style.css` | 스타일 |
| `GUIDE.md` | 코드 변경 후 항상 함께 업데이트 |
